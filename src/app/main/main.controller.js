(function(angular) {
  "use strict";

  angular.module("bidirApp").controller("MainController", MainController);
  /**@ngInject */
  function MainController(
    $state,
    AlertService,
    $scope,
    MainService,
    $uibModal,
    $log,
    $http
  ) {
    var vm = this;

  }
})(window.angular);
